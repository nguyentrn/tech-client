import Button from './Button';
import Heading from './Heading';
import Form from './Form';
import { StyledCard as Card } from './Card';

const components = { Button, Heading, Form, Card };

export default components;
