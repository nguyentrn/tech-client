import CenteredFlex from '../styles/components/CenteredFlex';

const Banner = (props: any) => {
  return (
    <CenteredFlex bg={'blackAlpha.700'} color={'white'} h={'80'} {...props} />
  );
};

export default Banner;
